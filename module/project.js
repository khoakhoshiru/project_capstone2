function Product(id, name, price, img, screen, backCamera, frontCamera, decs) {
  {
    this.id = id;
    this.name = name;
    this.price = price;
    this.screen = hinhAnh;
    this.backCamera = backCamera;
    this.frontCamera = frontCamera;
    this.img = img;
    this.decs = decs;
  }
}
